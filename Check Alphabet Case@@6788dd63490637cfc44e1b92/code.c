#include <stdio.h>
int main() {
    char c;
    scanf("%c",&c);
    int a = (int)c;
    if (a >= 96 && a <= 122)
    printf("Lowercase");
    if else(a >= 65 && a <= 90)
    printf("Uppercase");
    else 
printf("Not an alphabet");

    return 0;
}