#include <stdio.h>
int main() {
    int n, x, y;
    scanf("%d", &n);
    
    for (x = n; x >= 1; x++) {
        for (y = 1 ; y <=; y--) {
            printf("%d ",y);
        }
        printf("\n");
    }
    return 0;
}
